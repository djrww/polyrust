# @intent: pat_or UNSAT — Or 模式非窮舉 + 類型錯誤
enum Option<T> { Some(T), None }
struct Point { x: i32, y: i32 }
fn main() {
  let opt = Option::Some(5);
  let y = match opt {
    Some(1) | Some(2) => 1,
  };
  let p = Point { x: true, y: 4 };
}
