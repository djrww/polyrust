# @intent: unsafe + lifetime
fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
  if x.len() > y.len() { x } else { y }
}
fn main() {
  let a = 5;
  let p: *mut i32 = &mut a as *mut i32;
  unsafe {
    *p = 10;
  }
  let s1 = "hello";
  let s2 = "world!";
  let r = longest(s1, s2);
}
